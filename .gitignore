# Dependencies
node_modules/
.pnp
.pnp.*

# Prefer pnpm in this repo (pnpm-lock.yaml). Ignore alternate lockfiles if created by mistake.
package-lock.json
yarn.lock

# pnpm store must not live inside the repo; use global store-dir or ~/Library/pnpm/store
.pnpm-store/

# Next.js / JS build
.next/
.turbo/

# ESLint 9 flat-config cache (if generated locally)
.eslintcache
out/
build/
dist/

# Production / environments
*.local
.env*.local

# Python environments
venv/
.venv/
env/
.env/

# LiteRT checkpoints (~GB); optional default path repo/models/ (see LITERT_MODEL_PATH)
models/*.litertlm

# Python cache and build output
__pycache__/
*.py[cod]
*$py.class
*.so
develop-eggs/
downloads/
eggs/
.eggs/
# Python setuptools / local env (repo root only; allow Next.js runtimes under lib/)
/lib/*
!/lib/lite-sim/
!/lib/demo-player/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg

# Debug logs (package managers + Python)
npm-debug.log*
pnpm-debug.log*
yarn-debug.log*
yarn-error.log*
*.log
pip-log.txt
pip-delete-this-directory.txt

# TypeScript / Next toolchain
*.tsbuildinfo
next-env.d.ts

# IDE / OS
.vscode/
.idea/
*.swp
*.swo
.DS_Store
Thumbs.db

# Test and coverage artifacts
.htmlcov/
.tox/
.nox/
.coverage
.coverage.*
.cache
nosetests.xml
coverage.xml
*.cover
*.pywave

# Local simulation outputs and scratch assets
scratch/
snapshot_*.json
rescue_log_*.json
output_logs/
.tempmediaStorage/
media_*.jpg
media_*.png
media_*.mp4
media_*.webp
click_feedback_*.png

# Asset working files (raw uncompressed sources stay local)
public/models/_raw/
public/hdri/_raw/
*.blend
*.blend1

# Archives and private/internal notes
*.zip
*.tar.gz
.agent/
.claude/settings.local.json

# Accidental absolute-path mirror (local machine)
D/
